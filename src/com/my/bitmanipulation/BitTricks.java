package com.my.bitmanipulation;

public class BitTricks {

	/*
	1. Return the rightmost 1 in the binary representation of a number.
	x ^ (x & (x - 1))
	
	2. For a given array of repeated elements, exactly one element is not repeated. 
		You need to return the non-repeated element.
	n ^ n = 0
		n ^ 0 = n
	
	3. Write a function to determine the number of bits 
	required to convert integer A to integer B.
	
	*/
}
